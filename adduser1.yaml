- name: Create user with pre-hashed password
  hosts: all
  become: true
  tasks:
    - name: Create user with pre-hashed password
      ansible.builtin.user:
        name: "{{Username}}"
        password: "password"