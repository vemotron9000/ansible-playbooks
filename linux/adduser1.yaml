- name: Create user with password, man
  hosts: all
  become: true
  tasks:
    - name: Create user with password
      ansible.builtin.user:
        name: "{{Username}}"
        password: "{{ Password | password_hash('sha512', 'salt') }}"