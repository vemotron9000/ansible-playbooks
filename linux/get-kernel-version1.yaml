---
- name: Test connectivity to all VMs
  hosts: all
  gather_facts: yes
  tasks:
    
    - name: Kernel Check
      debug:
        var: ansible_kernel

    - name: Display Kernel Version
      debug:
        msg:
          - "Kernel: {{ ansible_result.stdout }}"