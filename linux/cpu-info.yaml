---
- name: Gets CPU Name
  hosts: all
  gather_facts: yes
  tasks:

    - name: Get CPU Model Name
      shell: cat /proc/cpuinfo |grep name
      register: cpu_result
      changed_when: false

    - name: Display CPU
      debug:
        msg:
          - "CPU: {{ cpu_result.stdout }}"